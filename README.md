## How to build

1. Pack the _contents_ of directory `res` into a zip archive.
2. Rename the archive into a `.jar` file.

## How to use with Guacamole docker image

Start the container with the `.jar` extension mounted into `/etc/guacamole/extensions`.
